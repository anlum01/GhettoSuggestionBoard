import React, { Component } from 'react';

const PlaceholderComponent = (props) => {
  return(
    <div>
      <h4>This is used for testing</h4>
    </div>
  );
};

export default PlaceholderComponent;
