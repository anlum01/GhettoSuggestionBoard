Please have `yarn` properly installed.

To run the app locally, run the script `start.sh`. The backend utilizes firebase credentials so make sure to create your
own `.env` file in the same directory to initialize firebase.


